NODE_ENV=development
PORT=3000

# "console" (dev default, logs events) | "noop" (prod default, silent)
ANALYTICS_ADAPTER=console

# --- Reserved for upcoming steps, not yet read by the app ---
# DATABASE_URL=postgres://user:password@localhost:5432/ecotrack
# JWT_SECRET=
